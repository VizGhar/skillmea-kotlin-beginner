# Basic Data Types

We consider the following as basic types: `Boolean`, `Int`, `Long`, `Float`, `Double`, `Char`, and `String`

Take a look at their usage in the video, and try to solve the following exercises. If you have any theoretical questions, turn to me or some LLM (artificial intelligence).

## 1.2.1 Int range

Print the largest and smallest 32-bit integer values (`Int`)

## 1.2.2 Long range

Print the largest and smallest 64-bit integer values (`Long`)

## 1.2.3 Int overflow

Print the absolute value of -200 and the absolute value of the smallest 32-bit integer

Why is there a problem with the smallest 32-bit integer? You'll find the answer in exercise 1

## 1.2.4 Int overflow 2

Convert `Int.MIN_VALUE` to `Long` using the `toLong()` function, and try to print the absolute value of this number

## 1.2.5 Surnames

Print the first letter of the text `"Nováková"` using `println()`

Print the last letter of the text `"Nováková"` using `println()`